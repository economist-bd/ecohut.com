import React, { useState, useEffect } from 'react';
import { initializeApp } from "firebase/app";
import { getAuth, signInWithCustomToken, signInAnonymously, onAuthStateChanged } from "firebase/auth";
import { getFirestore, collection, addDoc, serverTimestamp } from "firebase/firestore";
import { ShoppingCart, Phone, Mail, MapPin, CheckCircle, Star, Truck, ShieldCheck, Heart, Gift } from 'lucide-react';

// --- Firebase Setup ---
const firebaseConfig = JSON.parse(__firebase_config || '{}');
const app = initializeApp(firebaseConfig);
const auth = getAuth(app);
const db = getFirestore(app);
const appId = typeof __app_id !== 'undefined' ? __app_id : 'default-app-id';

// --- Main Component ---
export default function App() {
  const [user, setUser] = useState(null);
  const [loading, setLoading] = useState(false);
  const [orderSuccess, setOrderSuccess] = useState(false);
  
  // Form State
  const [formData, setFormData] = useState({
    name: '',
    phone: '',
    address: '',
    quantity: 1,
    deliveryArea: 'inside_dhaka'
  });

  // Product Info (Arabian Store BD Copied Data)
  const productPrice = 1250; 
  const regularPrice = 1490;
  const deliveryChargeInside = 70;
  const deliveryChargeOutside = 130;

  // Initialize Auth
  useEffect(() => {
    const initAuth = async () => {
      try {
        if (typeof __initial_auth_token !== 'undefined' && __initial_auth_token) {
          await signInWithCustomToken(auth, __initial_auth_token);
        } else {
          await signInAnonymously(auth);
        }
      } catch (error) {
        console.error("Auth failed:", error);
      }
    };
    initAuth();
    const unsubscribe = onAuthStateChanged(auth, setUser);
    return () => unsubscribe();
  }, []);

  const calculateTotal = () => {
    const delivery = formData.deliveryArea === 'inside_dhaka' ? deliveryChargeInside : deliveryChargeOutside;
    return (productPrice * formData.quantity) + delivery;
  };

  const handleInputChange = (e) => {
    const { name, value } = e.target;
    setFormData(prev => ({ ...prev, [name]: value }));
  };

  const handleQuantityChange = (delta) => {
    setFormData(prev => ({
      ...prev,
      quantity: Math.max(1, prev.quantity + delta)
    }));
  };

  const handleSubmit = async (e) => {
    e.preventDefault();
    if (!user) {
      alert("System is initializing, please try again.");
      return;
    }
    
    setLoading(true);

    try {
      await addDoc(collection(db, 'artifacts', appId, 'public', 'data', 'orders'), {
        ...formData,
        totalPrice: calculateTotal(),
        productName: "10 Items Chocolate Combo",
        orderDate: serverTimestamp(),
        status: 'new',
        userId: user.uid
      });

      setOrderSuccess(true);
      setFormData({
        name: '',
        phone: '',
        address: '',
        quantity: 1,
        deliveryArea: 'inside_dhaka'
      });
    } catch (error) {
      console.error("Error placing order:", error);
      alert("অর্ডার প্লেস করতে সমস্যা হয়েছে। দয়া করে আবার চেষ্টা করুন।");
    } finally {
      setLoading(false);
    }
  };

  if (orderSuccess) {
    return (
      <div className="min-h-screen bg-green-50 flex items-center justify-center p-4">
        <div className="bg-white p-8 rounded-2xl shadow-xl max-w-md w-full text-center border-t-4 border-green-600">
          <div className="w-20 h-20 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-6">
            <CheckCircle className="w-10 h-10 text-green-600" />
          </div>
          <h2 className="text-3xl font-bold text-gray-800 mb-4">অর্ডার কনফার্ম!</h2>
          <p className="text-gray-600 mb-6">আপনার অর্ডারটি সফলভাবে গ্রহণ করা হয়েছে। আমাদের প্রতিনিধি শীঘ্রই আপনার সাথে যোগাযোগ করবেন।</p>
          <button 
            onClick={() => setOrderSuccess(false)}
            className="bg-green-600 text-white px-6 py-3 rounded-lg font-semibold hover:bg-green-700 transition w-full shadow-lg"
          >
            আরো অর্ডার করুন
          </button>
        </div>
      </div>
    );
  }

  return (
    <div className="font-sans text-gray-800 bg-gray-50 min-h-screen pb-12">
      {/* Top Bar - Orange alert style often used in sales */}
      <div className="bg-orange-600 text-white py-2 px-4 text-sm font-medium text-center md:flex md:justify-between md:items-center container mx-auto">
        <p className="animate-pulse">সারা বাংলাদেশে ক্যাশ অন ডেলিভারি সুবিধা!</p>
        <div className="flex justify-center gap-4 mt-1 md:mt-0">
          <a href="tel:01715247588" className="flex items-center gap-1 hover:text-orange-200"><Phone size={14} /> 01715247588</a>
          <a href="mailto:himel452@gmail.com" className="hidden md:flex items-center gap-1 hover:text-orange-200"><Mail size={14} /> himel452@gmail.com</a>
        </div>
      </div>

      {/* Navbar */}
      <nav className="bg-white shadow-sm sticky top-0 z-50 border-b">
        <div className="container mx-auto px-4 py-3 flex justify-between items-center">
          <div className="flex items-center gap-2">
            {/* Simple Text Logo based on user request */}
            <h1 className="text-3xl font-extrabold text-green-700 tracking-tight">EcoHut</h1>
          </div>
          <a href="#order-form" className="bg-orange-600 text-white px-6 py-2 rounded-full font-bold text-sm hover:bg-orange-700 transition shadow-lg flex items-center gap-2">
            <ShoppingCart size={18} /> অর্ডার করুন
          </a>
        </div>
      </nav>

      {/* Hero Section */}
      <div className="bg-white py-8 md:py-12">
        <div className="container mx-auto px-4">
          <div className="flex flex-col md:flex-row gap-8 items-start">
            
            {/* Image Side */}
            <div className="w-full md:w-1/2 sticky top-24">
               <div className="relative aspect-square bg-gray-100 rounded-xl overflow-hidden shadow-lg border border-gray-200">
                 <img 
                   src="https://images.unsplash.com/photo-1623910398642-83b6f8497678?q=80&w=1000&auto=format&fit=crop" 
                   alt="Chocolate Combo" 
                   className="w-full h-full object-cover hover:scale-105 transition duration-700"
                 />
                 <div className="absolute top-4 left-4 bg-red-600 text-white px-4 py-1 rounded-full font-bold shadow-md text-sm">
                   16% OFF
                 </div>
               </div>
               <div className="mt-4 grid grid-cols-4 gap-2">
                 {[1,2,3,4].map((i) => (
                   <div key={i} className="aspect-square bg-gray-100 rounded-lg overflow-hidden border hover:border-orange-500 cursor-pointer">
                      <img src={`https://images.unsplash.com/photo-1549007994-cb92caebd54b?w=200&q=80`} className="w-full h-full object-cover" alt="thumbnail" />
                   </div>
                 ))}
               </div>
            </div>

            {/* Content Side (Text from Arabian Store BD) */}
            <div className="w-full md:w-1/2 space-y-6">
              <div>
                <h1 className="text-3xl md:text-4xl font-bold text-gray-900 leading-tight mb-2">
                  ১০ আইটেম চকলেট কম্বো!
                </h1>
                <div className="flex items-center gap-2 mb-4">
                   <div className="flex text-yellow-400">
                     <Star fill="currentColor" size={18} />
                     <Star fill="currentColor" size={18} />
                     <Star fill="currentColor" size={18} />
                     <Star fill="currentColor" size={18} />
                     <Star fill="currentColor" size={18} />
                   </div>
                   <span className="text-gray-500 text-sm">(১৫০+ রিভিউ)</span>
                </div>
                
                <div className="flex items-end gap-3 mb-6 border-b pb-6">
                  <span className="text-4xl font-bold text-orange-600">৳{productPrice}</span>
                  <span className="text-xl text-gray-400 line-through mb-1">৳{regularPrice}</span>
                  <span className="bg-red-100 text-red-600 px-2 py-1 rounded text-xs font-bold mb-2">Save ৳{regularPrice - productPrice}</span>
                </div>
              </div>

              {/* The Emotional Pitch */}
              <div className="prose text-gray-700 space-y-4">
                <p className="font-medium text-lg text-gray-800">
                  আপনার সন্তান কী চকলেটে আসক্ত? কিংবা আপনার প্রিয়জনের কী চকলেট প্রিয়?
                </p>
                <p>
                  জ্বী! চকলেট আমাদের কার না প্রিয়। তবে বাজারের নিম্নমানের ক্যামিক্যাল নামক চকলেট কী আপনার সন্তান কিংবা প্রিয়জনকে তৃপ্ত করতে পারবে? এসব চকলেট কী তাদের স্বাস্থ্য ঝুকিতে ফেলে দিচ্ছে না?
                </p>
                <p className="bg-green-50 p-4 rounded-lg border-l-4 border-green-500 italic">
                  তাই তো আমরা আপনার জন্য ও আপনার সন্তান-প্রিয়জনদের সুস্বাস্থ্য রক্ষায় বিশ্ব মানের <span className="font-bold text-green-700">১০ আইটেম চকলেট কম্বো</span> নিয়ে হাজির হলাম। যা চমকে দিবে আপনার প্রিয়জনকে। নিরাপদ রাখবে আপনার সন্তানের স্বাস্থ্য।
                </p>
              </div>

              {/* Why Buy From Us */}
              <div className="bg-gray-50 p-5 rounded-xl border border-gray-200">
                <h3 className="font-bold text-xl mb-3 flex items-center gap-2"><CheckCircle className="text-green-600" size={20}/> আমাদের থেকে কেন নিবেন!</h3>
                <ul className="space-y-2 text-gray-700">
                  <li className="flex items-start gap-2">
                    <span className="text-green-500 mt-1">✔</span> সৌদি, দুবাই, তুরস্ক, চীন থেকে আমদানিকৃত।
                  </li>
                  <li className="flex items-start gap-2">
                    <span className="text-green-500 mt-1">✔</span> ১০ আইটেমের ১০ টি সেরা স্বাদের চকলেট।
                  </li>
                  <li className="flex items-start gap-2">
                    <span className="text-green-500 mt-1">✔</span> খেজুর চকলেট, যা বাদাম মিশ্রিত।
                  </li>
                </ul>
              </div>

              {/* Why Gift This */}
              <div>
                <h3 className="font-bold text-xl mb-3 flex items-center gap-2"><Gift className="text-orange-600" size={20}/> এ চকলেট আপনার প্রিয়জনকে কেন দিবেন?</h3>
                <ul className="grid grid-cols-1 gap-2 text-sm text-gray-700">
                  <li className="flex items-center gap-2 bg-white p-2 border rounded shadow-sm">
                    <Heart size={16} className="text-red-500" /> এ কম্বোতে রয়েছে খেজুরের চকলেটসহ নানা চকলেট যা পুষ্টি ঘাটতি পূরণ করবে।
                  </li>
                  <li className="flex items-center gap-2 bg-white p-2 border rounded shadow-sm">
                    <ShieldCheck size={16} className="text-blue-500" /> দোকান-পাট বা বাহিরের যে কোনো খাবার থেকে আসক্তি দূর করবে।
                  </li>
                  <li className="flex items-center gap-2 bg-white p-2 border rounded shadow-sm">
                    <Star size={16} className="text-yellow-500" /> বাহিরের খাবারের জন্য কান্না করলেই নিরাপদ এ চকলেট দিয়ে কান্না থামিয়ে দিতে পারবে।
                  </li>
                  <li className="flex items-center gap-2 bg-white p-2 border rounded shadow-sm">
                    <Gift size={16} className="text-purple-500" /> যে কোনো ভালো কাজ করলে একটি চকলেট কম্বো গিফট করে উৎসাহিত করুন।
                  </li>
                </ul>
              </div>

              <div className="pt-4">
                <a href="#order-form" className="block w-full text-center bg-orange-600 text-white py-4 rounded-xl font-bold text-xl shadow-lg hover:bg-orange-700 transition transform hover:-translate-y-1 animate-bounce">
                  অর্ডার করতে এখানে ক্লিক করুন
                </a>
                <p className="text-center text-gray-500 text-sm mt-3">অথবা কল করুন: <span className="font-bold text-gray-800">01715247588</span></p>
              </div>
            </div>
          </div>
        </div>
      </div>

      {/* Order Form Section */}
      <div id="order-form" className="py-12 bg-gray-100">
        <div className="container mx-auto px-4">
          <div className="max-w-2xl mx-auto bg-white rounded-xl shadow-xl overflow-hidden border border-orange-200">
            <div className="bg-gray-800 py-4 text-center">
              <h2 className="text-2xl font-bold text-white">অর্ডার কনফার্ম করতে নিচের ফর্মটি পূরণ করুন</h2>
            </div>
            
            <form onSubmit={handleSubmit} className="p-6 md:p-8 space-y-5">
              
              {/* Product Summary within form */}
              <div className="flex justify-between items-center bg-orange-50 p-4 rounded-lg border border-orange-100">
                <div className="flex items-center gap-3">
                   <img src="https://images.unsplash.com/photo-1623910398642-83b6f8497678?q=80&w=100" alt="small thumb" className="w-12 h-12 rounded object-cover border" />
                   <div>
                      <p className="font-semibold text-gray-800">10 Item Chocolate Combo</p>
                      <p className="text-xs text-gray-500">Premium Quality</p>
                   </div>
                </div>
                <span className="font-bold text-orange-600 text-lg">৳{productPrice}</span>
              </div>

              <div className="space-y-4">
                <div>
                  <label className="block text-sm font-bold text-gray-700 mb-1">আপনার নাম *</label>
                  <input
                    required
                    type="text"
                    name="name"
                    value={formData.name}
                    onChange={handleInputChange}
                    placeholder="আপনার সম্পূর্ণ নাম"
                    className="w-full px-4 py-3 rounded border border-gray-300 focus:ring-2 focus:ring-orange-500 focus:border-orange-500 outline-none transition bg-gray-50"
                  />
                </div>

                <div>
                  <label className="block text-sm font-bold text-gray-700 mb-1">মোবাইল নাম্বার *</label>
                  <input
                    required
                    type="tel"
                    name="phone"
                    value={formData.phone}
                    onChange={handleInputChange}
                    placeholder="017XXXXXXXX"
                    className="w-full px-4 py-3 rounded border border-gray-300 focus:ring-2 focus:ring-orange-500 focus:border-orange-500 outline-none transition bg-gray-50"
                  />
                </div>

                <div>
                  <label className="block text-sm font-bold text-gray-700 mb-1">সম্পূর্ণ ঠিকানা *</label>
                  <textarea
                    required
                    name="address"
                    value={formData.address}
                    onChange={handleInputChange}
                    rows="2"
                    placeholder="বাসা নং, রোড নং, এলাকা/গ্রাম, থানা, জেলা"
                    className="w-full px-4 py-3 rounded border border-gray-300 focus:ring-2 focus:ring-orange-500 focus:border-orange-500 outline-none transition bg-gray-50"
                  />
                </div>

                {/* Quantity */}
                <div>
                   <label className="block text-sm font-bold text-gray-700 mb-2">পরিমাণ</label>
                   <div className="flex items-center gap-4">
                      <button type="button" onClick={() => handleQuantityChange(-1)} className="w-10 h-10 rounded border hover:bg-gray-100 flex items-center justify-center text-xl font-bold text-gray-600">-</button>
                      <span className="text-xl font-bold w-12 text-center">{formData.quantity}</span>
                      <button type="button" onClick={() => handleQuantityChange(1)} className="w-10 h-10 rounded border hover:bg-gray-100 flex items-center justify-center text-xl font-bold text-gray-600">+</button>
                   </div>
                </div>

                <div>
                  <label className="block text-sm font-bold text-gray-700 mb-2">ডেলিভারি এলাকা নির্বাচন করুন *</label>
                  <div className="space-y-2">
                    <label className={`cursor-pointer border rounded p-3 flex items-center justify-between transition ${formData.deliveryArea === 'inside_dhaka' ? 'border-orange-500 bg-orange-50' : 'border-gray-200 hover:bg-gray-50'}`}>
                      <div className="flex items-center gap-3">
                        <input
                          type="radio"
                          name="deliveryArea"
                          value="inside_dhaka"
                          checked={formData.deliveryArea === 'inside_dhaka'}
                          onChange={handleInputChange}
                          className="w-4 h-4 text-orange-600 focus:ring-orange-500"
                        />
                        <span className="font-medium text-gray-700">ঢাকার ভিতরে</span>
                      </div>
                      <span className="font-bold text-gray-600">৳{deliveryChargeInside}</span>
                    </label>

                    <label className={`cursor-pointer border rounded p-3 flex items-center justify-between transition ${formData.deliveryArea === 'outside_dhaka' ? 'border-orange-500 bg-orange-50' : 'border-gray-200 hover:bg-gray-50'}`}>
                      <div className="flex items-center gap-3">
                        <input
                          type="radio"
                          name="deliveryArea"
                          value="outside_dhaka"
                          checked={formData.deliveryArea === 'outside_dhaka'}
                          onChange={handleInputChange}
                          className="w-4 h-4 text-orange-600 focus:ring-orange-500"
                        />
                        <span className="font-medium text-gray-700">ঢাকার বাইরে</span>
                      </div>
                      <span className="font-bold text-gray-600">৳{deliveryChargeOutside}</span>
                    </label>
                  </div>
                </div>
              </div>

              {/* Total Summary */}
              <div className="bg-gray-50 p-4 rounded border border-gray-200 mt-2">
                 <div className="flex justify-between items-center mb-1 text-sm">
                   <span className="text-gray-600">সাবটোটাল</span>
                   <span className="font-medium">৳{productPrice * formData.quantity}</span>
                 </div>
                 <div className="flex justify-between items-center mb-2 text-sm">
                   <span className="text-gray-600">ডেলিভারি চার্জ</span>
                   <span className="font-medium">৳{formData.deliveryArea === 'inside_dhaka' ? deliveryChargeInside : deliveryChargeOutside}</span>
                 </div>
                 <div className="flex justify-between items-center text-xl font-bold text-gray-800 border-t border-gray-300 pt-2">
                   <span>সর্বমোট</span>
                   <span className="text-orange-600">৳{calculateTotal()}</span>
                 </div>
              </div>

              <button
                type="submit"
                disabled={loading}
                className={`w-full bg-orange-600 text-white font-bold text-lg py-4 rounded shadow hover:bg-orange-700 transition transform active:scale-95 flex justify-center items-center gap-2 ${loading ? 'opacity-70 cursor-not-allowed' : ''}`}
              >
                {loading ? 'প্রসেসিং...' : <span>অর্ডার কনফার্ম করুন <span className="text-sm font-normal opacity-80">(৳{calculateTotal()})</span></span>}
              </button>
            </form>
          </div>
        </div>
      </div>

      {/* Footer */}
      <footer className="bg-gray-900 text-gray-400 py-8 border-t border-gray-800">
        <div className="container mx-auto px-4 text-center">
           <h3 className="text-2xl font-bold text-white mb-4">EcoHut</h3>
           <div className="flex justify-center gap-6 mb-6">
             <a href="tel:01715247588" className="hover:text-white transition">01715247588</a>
             <a href="mailto:himel452@gmail.com" className="hover:text-white transition">himel452@gmail.com</a>
           </div>
           <p className="text-sm">&copy; 2024 EcoHut. All rights reserved.</p>
        </div>
      </footer>
    </div>
  );
}
